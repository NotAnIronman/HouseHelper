{
  "name": "househelper",
  "version": "0.1.0",
  "private": true,
  "description": "Touch-first family dashboard and PWA",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node --check dist/app.js && node --check dist/service-worker.js && node --check scripts/serve.mjs"
  }
}
